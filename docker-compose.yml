version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "15722:15722"
    environment:
      - MOCK_MODE=true
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: unless-stopped
